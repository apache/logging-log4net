<?xml version="1.0" encoding="UTF-8"?>
<entry xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="https://logging.apache.org/xml/ns"
       xsi:schemaLocation="https://logging.apache.org/xml/ns https://logging.apache.org/xml/ns/log4j-changelog-0.xsd"
       type="fixed">
  <issue id="162" link="https://github.com/apache/logging-log4net/issues/162"/>
  <description format="asciidoc">Stop reporting `log4net:ERROR Exception while reading ConfigurationSettings`
  in a process that hosts the runtime natively, such as `powershell.exe` or a C++ host of the CoreCLR.
  There is no entry assembly there for the configuration system to derive the config file path from, so
  it fails with `PlatformNotSupportedException` before any config file is read. That is now recognised
  as an absent configuration system, the same as under Native AOT: it is logged once at debug level and
  application settings are read from environment variables instead
  (reported by @viktorgobbi, fixed by @FreeAndNil)</description>
</entry>
