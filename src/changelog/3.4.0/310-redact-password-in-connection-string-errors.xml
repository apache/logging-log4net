<?xml version="1.0" encoding="UTF-8"?>
<entry xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="https://logging.apache.org/xml/ns"
       xsi:schemaLocation="https://logging.apache.org/xml/ns https://logging.apache.org/xml/ns/log4j-changelog-0.xsd"
       type="fixed">
  <issue id="310" link="https://github.com/apache/logging-log4net/pull/310"/>
  <description format="asciidoc">
    stop `AdoNetAppender` from repeating the password when it reports a connection it could not
 open. The message named the resolved connection string in full, and the documented examples embed
 `Password=...` (CWE-532). Password-bearing keywords are now replaced with `*****`, while the rest
 of the connection string is kept so the message stays useful (audit 1231d72-f018)
  </description>
</entry>
