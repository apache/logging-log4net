<?xml version="1.0" encoding="UTF-8"?>
<entry xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="https://logging.apache.org/xml/ns"
       xsi:schemaLocation="https://logging.apache.org/xml/ns https://logging.apache.org/xml/ns/log4j-changelog-0.xsd"
       type="changed">
  <issue id="323" link="https://github.com/apache/logging-log4net/pull/323"/>
  <description format="asciidoc">Deprecate `log4net.Util.TwoArgAction`. It existed for the callback
  the logical context stack used to register itself, which now holds its owning
  `LogicalThreadContextStacks` instead, so nothing in log4net uses the delegate. It will be removed
  in version 4 (implemented by @FreeAndNil)</description>
</entry>
