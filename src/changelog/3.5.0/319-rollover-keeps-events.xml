<?xml version="1.0" encoding="UTF-8"?>
<entry xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="https://logging.apache.org/xml/ns"
       xsi:schemaLocation="https://logging.apache.org/xml/ns https://logging.apache.org/xml/ns/log4j-changelog-0.xsd"
       type="fixed">
  <issue id="319" link="https://github.com/apache/logging-log4net/pull/319"/>
  <description format="asciidoc">Keep the log file when a rollover cannot rename it. The failed
  rename was reported and the file then reopened without appending, which destroyed everything it
  held; a reader holding the file without `FILE_SHARE_DELETE`, such as a backup or antivirus agent,
  is enough to cause it. The file is kept and appended to now, at startup as well, and the rename is
  retried as the file grows, so it can exceed `MaxFileSize` while the rename keeps failing (audit
  da18b6fd-f036, fixed by @FreeAndNil)</description>
</entry>
