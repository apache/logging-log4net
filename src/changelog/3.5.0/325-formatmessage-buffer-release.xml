<?xml version="1.0" encoding="UTF-8"?>
<entry xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="https://logging.apache.org/xml/ns"
       xsi:schemaLocation="https://logging.apache.org/xml/ns https://logging.apache.org/xml/ns/log4j-changelog-0.xsd"
       type="changed">
  <issue id="325" link="https://github.com/apache/logging-log4net/pull/325"/>
  <description format="asciidoc">
    release the `FormatMessage` buffer in `NativeError` with `LocalFree`, the allocator that API
 uses, instead of letting the marshaller free it as COM task memory. Both hit the process heap on
 current Windows, so this is hygiene rather than a fix (audit da18b6fd-f045, fixed by @FreeAndNil)
  </description>
</entry>
