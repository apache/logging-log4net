<?xml version="1.0" encoding="UTF-8"?>
<entry xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="https://logging.apache.org/xml/ns"
       xsi:schemaLocation="https://logging.apache.org/xml/ns https://logging.apache.org/xml/ns/log4j-changelog-0.xsd"
       type="fixed">
  <issue id="319" link="https://github.com/apache/logging-log4net/pull/319"/>
  <description format="asciidoc">Let two processes that spell the log path
  differently share one file lock. The lock was named after the configured path before that path was
  resolved, so a relative and an absolute spelling of one file took two different locks and excluded
  nothing. The resolved path names it now. Only that spelling is covered: symbolic links, hard
  links, 8.3 short names, letter case and UNC versus mapped-drive spellings still count as different
  files, and on Windows a service and a desktop application never share the lock at
  all (audit da18b6fd-f031, fixed by @FreeAndNil)</description>
</entry>
