<?xml version="1.0" encoding="UTF-8"?>
<entry xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns="https://logging.apache.org/xml/ns"
       xsi:schemaLocation="https://logging.apache.org/xml/ns https://logging.apache.org/xml/ns/log4j-changelog-0.xsd"
       type="fixed">
  <issue id="319" link="https://github.com/apache/logging-log4net/pull/319"/>
  <description format="asciidoc">Stop a lock the appender could not take from
  disabling it. Writing the footer, closing the writer and opening the file released the file lock
  even when taking it had failed, and every later attempt then failed too, so the footer and
  anything still buffered were dropped without a word. Only a lock that was taken is released
  now (audit da18b6fd-f032, fixed by @FreeAndNil)</description>
</entry>
